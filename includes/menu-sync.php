<?php
/**
 * ==========================================================
 * MENÚ ADMINISTRATIVO: Sincronización (Core)
 * ==========================================================
 * Archivo base para futuras integraciones de sincronización.
 * Ya no genera menús duplicados; las páginas se registran
 * desde menu-reservas.php para mantener una sola jerarquía.
 * ==========================================================
 */

if (!defined('ABSPATH')) exit;

/**
 * Este archivo queda reservado para manejar lógica de
 * sincronización avanzada (por ejemplo, conexiones automáticas
 * con APIs externas, logs o tareas CRON relacionadas).
 *
 * Ya no debe incluir ningún add_menu_page() o add_submenu_page().
 */
